import "package:flutter/material.dart";

class DemoDoctor extends StatelessWidget {
  const DemoDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Demo Doctor")),
    );
  }
}
